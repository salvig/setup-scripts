TMP_FOLDER=$(mktemp -d)
CONFIGFOLDER=$HOME/.paccoincore
CONFIG_FILE='paccoin.conf'
COIN_DAEMON='paccoind'
COIN_CLI='paccoin-cli'
COIN_PATH='/usr/local/bin/'
COIN_NAME='paccoin'
COIN_PORT=7112
RPC_PORT=7111
COIN_TGZ=$(curl -s https://api.github.com/repos/PACCommunity/PAC/releases/latest | grep browser_download_url | grep -e "linux-x86_64"| cut -d '"' -f 4)
COIN_ZIP=$(echo $COIN_TGZ | awk -F'/' '{print $NF}')
